services:
  - type: web
    name: azrireel
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    healthCheckPath: /
    autoDeploy: true
    disk:
      name: azrireel-data
      mountPath: /app/output
      sizeGB: 5
